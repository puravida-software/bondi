name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release

jobs:
  release:
    name: build-${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            platform: linux-x86_64
          # Disable for now to save on CI credits (remember to update the action and formula)
          # - os: macos-14
          #   platform: macos-arm64

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: extractions/setup-just@v3
        with:
          just-version: "1.46.0"
      - uses: ./.github/actions/ocaml-deps

      - name: Set version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          if [ -z "$VERSION" ]; then
            echo "::error::Could not derive version from tag (GITHUB_REF_NAME=$GITHUB_REF_NAME)"
            exit 1
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Build CLI
        run: |
          export BONDI_VERSION="${VERSION}"
          opam exec -- dune build @install
          opam exec -- dune install --prefix "$PWD/_build/install"

      - name: Package artifact
        run: |
          dist_dir="$PWD/dist"
          mkdir -p "$dist_dir"
          install -m 755 "_build/install/default/bin/bondi-client" "$dist_dir/bondi"
          tar -czf "bondi-${{ matrix.platform }}.tar.gz" -C "$dist_dir" bondi

      - name: Publish release asset
        uses: softprops/action-gh-release@v2
        with:
          files: bondi-${{ matrix.platform }}.tar.gz

  release-server:
    name: release-server-image
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6

      - name: Set version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          if [ -z "$VERSION" ]; then
            echo "::error::Could not derive version from tag (GITHUB_REF_NAME=$GITHUB_REF_NAME)"
            exit 1
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: extractions/setup-just@v3
        with:
          just-version: "1.46.0"

      - name: Build server image
        run: |
          just build-server "$VERSION"
          just tag-server "$VERSION"

      # Nothing is pushed until the built image has been proven to run. The
      # assertions target the local tag produced above, which `push-server`
      # then uploads, so the image that is checked and the image that is
      # published are the same artifact rather than two builds of one
      # Dockerfile. Versions 0.8.2 through 0.10.1 built cleanly and could not
      # start; this is the step that would have caught them.
      - name: Verify the built image runs
        run: just verify-server-image "$VERSION"

      # A check that cannot fail is not a check.
      - name: Verify that the check itself can fail
        run: just verify-server-image-negative "$VERSION"

      - name: Push server image
        run: |
          just push-server "$VERSION"
          docker push mlopez1506/bondi-server:latest

  update-homebrew-tap:
    name: update-homebrew-tap
    runs-on: ubuntu-22.04
    needs: release
    env:
      TAP_REPO: puravida-software/homebrew-bondi
      FORMULA_PATH: Formula/bondi.rb
    steps:
      - name: Checkout main repository
        uses: actions/checkout@v6
        with:
          path: main
          fetch-depth: 0

      - name: Checkout tap repository
        uses: actions/checkout@v6
        with:
          repository: ${{ env.TAP_REPO }}
          token: ${{ secrets.TAP_GITHUB_TOKEN }}
          path: tap

      - name: Download release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p "$RUNNER_TEMP/release"
          gh release download "${GITHUB_REF_NAME}" \
            -R "${GITHUB_REPOSITORY}" \
            -p "bondi-*.tar.gz" \
            -D "$RUNNER_TEMP/release"

      - name: Set version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          if [ -z "$VERSION" ]; then
            echo "::error::Could not derive version from tag (GITHUB_REF_NAME=$GITHUB_REF_NAME)"
            exit 1
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Update formula
        uses: ./main/.github/actions/update-homebrew-formula
        with:
          version: ${{ env.VERSION }}
          artifacts-path: ${{ runner.temp }}/release
          formula-path: tap/${{ env.FORMULA_PATH }}

      - name: Commit and push
        working-directory: tap
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "${FORMULA_PATH}"
          git commit -m "chore: update bondi formula to ${GITHUB_REF_NAME}" || exit 0
          git push
